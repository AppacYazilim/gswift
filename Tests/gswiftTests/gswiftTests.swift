import XCTest
@testable import gswift

final class gswiftTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(GParser().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
